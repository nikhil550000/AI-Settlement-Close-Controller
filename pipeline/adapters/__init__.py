"""Bank statement adapter: declarative per-profile column maps
(`pipeline/adapters/profiles/*.yaml`) plus the parser that reads any of
the three profiles into the canonical `bank_line` shape (`pipeline.schemas.BankLine`).
"""
